my.db {
  dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
  properties = {
    driver = "org.postgresql.Driver"
    url = "postgres://postgres:postgres@postgres/mydb"
  }
  minConnections = 5
  maxConnections = 10
  connectionTimeout = 5000
}